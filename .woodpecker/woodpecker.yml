when:
  - event: push
    branch: Master

steps:
  - name: build
    image: pico-build:latest
    commands:
      - mkdir build
      - cd build
      - cmake ..
      - make -j4
    privileged: true
    volumes:
      - ~/Programs/Downloaded/Pico/Libs:/libs
